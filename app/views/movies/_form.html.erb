<%= form_for :movie, url: url, method: mthd do |f| %>
    <div class="form-group">
      <%= f.label :title, 'Title', :class => 'form-label' %>
      <%= f.text_field :title, :class => 'form-control' %>
    </div>

    <%= f.submit :class => 'btn btn-submit' %>
<% end %>